{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build with g++-15",
            "type": "shell",
            "command": "/opt/homebrew/bin/g++-15",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${workspaceFolder}/a.out"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}